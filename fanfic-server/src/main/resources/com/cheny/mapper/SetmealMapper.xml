<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cheny.mapper.SetmealMapper">

    <resultMap id="BaseResultMap" type="com.cheny.entity.Setmeal">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="categoryId" column="category_id" />
            <result property="price" column="price" />
            <result property="image" column="image" />
            <result property="description" column="description" />
            <result property="status" column="status" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="createUser" column="create_user" />
            <result property="updateUser" column="update_user" />
    </resultMap>

    <sql id="Base_Column_List">
        id,name,category_id,price,image,description,
        status,create_time,update_time,create_user,update_user
    </sql>

    <select id = "selectPageSetmeal" resultType = "com.cheny.vo.SetmealVo">
        select
        id,name,category_id,price,image,description,status,update_time
        from setmeal
        ${ew.customSqlSegment}

    </select>
    <select id = "getDishItemBySetmealId" resultType = "com.cheny.vo.DishItemVo">
        select
        id,name,category_id,price,image,description,status,update_time
        from setmeal_dish sd left join dish d on sd.dish_id = d.id
        ${ew.customSqlSegment}

    </select>
</mapper>
